FROM python:3.11-slim

ENV DEBIAN_FRONTEND=noninteractive

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    libreoffice \
 && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .

RUN pip install --no-cache-dir --upgrade pip setuptools wheel && \
    pip install --no-cache-dir -r requirements.txt

COPY . .

RUN chmod +x docker/entrypoint.sh || true

# 🔥 FIX ICI
RUN mkdir -p /app/data && chmod -R 777 /app/data

VOLUME ["/var/log"]

EXPOSE 8080

CMD ["./docker/entrypoint.sh"]



apiVersion: v1
kind: Service
metadata:
  name: deepresearch-api
  namespace: deepresearch
spec:
  selector:
    app: deepresearch-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP


http://deepresearch-api.deepresearch.svc.cluster.local:8000



oc run curltest --rm -it --image=curlimages/curl:8.7.1 -n deepresearch -- \
  curl -v http://deepresearch-api:8000


oc run curltest --rm -it --image=curlimages/curl:8.7.1 -n <namespace-apigee> -- \
  curl -v http://deepresearch-api.deepresearch.svc.cluster.local:8000
